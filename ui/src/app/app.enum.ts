export enum BaseUrl {
    BASE_URL = "http://172.16.35.40:8089/api/login"
}

export enum appConstant {
    SUCCESS = "Success",
    ERROR = "Error",
    CLOSE = "Close",
    OK = "Ok"
}

export enum urlConstant {
    LOGIN = "login",
    NON_SEC = "nonsec",
    SEC = "sec"
}
